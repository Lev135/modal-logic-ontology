#include "../core/cong_logics.dl"

.comp InterpolationComp {
  .decl sat_CIP(l : Logic)
  .decl sat_LIP(l : Logic)
  .decl nsat_CIP(l : Logic)
  .decl nsat_LIP(l : Logic)

  sat_CIP(l) :- sat_LIP(l).
  nsat_LIP(l) :- nsat_CIP(l).
}
.init Interpolation = InterpolationComp

Interpolation.nsat_CIP($L("K4.3")).
Interpolation.sat_LIP($L("K")).
